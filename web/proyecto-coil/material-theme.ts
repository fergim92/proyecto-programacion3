import { createTheme } from "@mui/material/styles";

export const lightTheme = createTheme({
  palette: {
    mode: "light",
    // Aquí puedes definir los colores y estilos para el tema claro
  },
});

export const darkTheme = createTheme({
  palette: {
    mode: "dark",
    // Aquí puedes definir los colores y estilos para el tema oscuro
  },
});
